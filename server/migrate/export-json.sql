COPY (SELECT row_to_json(t) FROM authors as t) to '/tmp/authors.json';
COPY (SELECT row_to_json(t) FROM events as t) to '/tmp/events.json';
COPY (SELECT row_to_json(t) FROM libraries as t) to '/tmp/libraries.json';
COPY (SELECT row_to_json(t) FROM message_files as t) to '/tmp/message_files.json';
COPY (SELECT row_to_json(t) FROM message_sets as t) to '/tmp/message_sets.json';
COPY (SELECT row_to_json(t) FROM messages as t) to '/tmp/messages.json';
COPY (SELECT row_to_json(t) FROM reservations as t) to '/tmp/reservations.json';
COPY (SELECT row_to_json(t) FROM resources as t) to '/tmp/resources.json';
COPY (SELECT row_to_json(t) FROM users as t) to '/tmp/users.json';
COPY (SELECT row_to_json(t) FROM audios as t) to '/tmp/audios.json';
COPY (SELECT row_to_json(t) FROM documents as t) to '/tmp/documents.json';
COPY (SELECT row_to_json(t) FROM forms as t) to '/tmp/forms.json';
COPY (SELECT row_to_json(t) FROM form_sections as t) to '/tmp/form_sections.json';
COPY (SELECT row_to_json(t) FROM form_fields as t) to '/tmp/form_fields.json';
COPY (SELECT row_to_json(t) FROM form_field_options as t) to '/tmp/form_field_options.json';
COPY (SELECT row_to_json(t) FROM filled_forms as t) to '/tmp/filled_forms.json';
COPY (SELECT row_to_json(t) FROM filled_fields as t) to '/tmp/filled_fields.json';
COPY (SELECT row_to_json(t) FROM filled_field_options as t) to '/tmp/filled_field_options.json';
COPY (SELECT row_to_json(t) FROM newsletters as t) to '/tmp/newsletters.json';
COPY (SELECT row_to_json(t) FROM payments as t) to '/tmp/payments.json';
COPY (SELECT row_to_json(t) FROM pages as t) to '/tmp/pages.json';
COPY (SELECT row_to_json(t) FROM contacts as t) to '/tmp/contacts.json';
COPY (SELECT row_to_json(t) FROM photos as t) to '/tmp/photos.json';
COPY (SELECT row_to_json(t) FROM videos as t) to '/tmp/videos.json';
COPY (SELECT row_to_json(t) FROM styles as t) to '/tmp/styles.json';
